# Agent instructions — ciscolive26

The exported Codex conversation archive was intentionally removed from this
repository. Do not assume prior conversation context from `codex-archive/`.

Use the checked-in README, workshop docs, architecture docs, source code, and
current git history as the source of truth for this project.
